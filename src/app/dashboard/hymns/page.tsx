import { PlaceholderSection } from "@/components/placeholder-section"

export default function HymnsPage() {
  return (
    <PlaceholderSection 
      title="Hymn Management"
      description="Manage your church's hymn library and song selections. Organize hymns by category, create playlists, and integrate with service planning."
    />
  )
}